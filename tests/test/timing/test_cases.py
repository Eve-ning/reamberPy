from tests.test.timing.cases.cases import *


cases_meta = {
    "Reseat Case 0": case_reseat_0,
    "Reseat Case 1": case_reseat_1,
}
cases_id = list(cases_meta.keys())
cases = list(cases_meta.values())
